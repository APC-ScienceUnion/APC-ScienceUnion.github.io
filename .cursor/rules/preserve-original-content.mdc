---
description: 禁止未经用户同意修改原文内容；格式转换须忠实保留原文字句
alwaysApply: true
---

# 保留原文，禁止擅自改写

处理本仓库中的用户原创内容（尤其是 `source/_posts/` 下的文章、用户提供的 Word/文稿原文）时，**不得擅自修改原文本身**。

## 禁止擅自修改的内容

未经用户**明确要求**，不得改动：

- 措辞、语序、标点
- 段落划分与结构
- 加粗、列表等 Markdown 格式（除非用户要求或仅为等价格式转换）
- 以“润色”“通顺”“修复语病”“统一风格”为理由的任何改写

## 允许的改动（须尽量最小化）

仅在用户任务需要时，才做**技术性、可说明**的转换，并**忠实保留原文字句**：

- Hexo front matter、分类、标签、日期等元数据（按用户指示填写）
- 图片路径、引用链接等发布所需路径
- 公式：将 Word/图片公式转为 KaTeX/MathJax（**不改变公式含义与编号**）
- 用户明确要求的组件（如 `{% note %}` 引言块）

## 必须告知用户

若发现原文存在明显问题（语病、重复段落、格式冲突），应：

1. **先说明**发现了什么、建议如何改
2. **等用户确认**后再改原文
3. 不得静默“优化”后提交

## 格式转换示例

```markdown
# ❌ 错误：擅自润色
原文：研究自然对流散热问题能更好的指导我们设计改进电子产品
改为：研究自然对流散热问题能更好地指导我们设计并改进电子产品

# ✅ 正确：保留原句，仅做发布格式
&emsp;&emsp;研究自然对流散热问题能更好的指导我们设计改进电子产品。
```

```markdown
# ❌ 错误：擅自调整结构与强调
- 将三步说明改为 1. 2. 3. 列表
- 自行添加 **加粗** 或重写小标题

# ✅ 正确
- 按原文段落顺序与表述还原
- 只补充 Hexo 渲染所需的语法（标题 `#`、缩进、图片标签）
```

## 默认原则

**用户的文字以用户为准。** 宁可保留原文中的表述，也不要替用户“改好”。只有用户说“可以改”“帮我润色”“按你的建议改”时，才可修改正文。
